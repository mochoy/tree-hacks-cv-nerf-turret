#define PUSHER_MOTOR_PIN 3
#define PUSHER_BRAKE_PIN 4
#define FLYWHEEL_MOTOR_PIN 5

void setup() {
	pinMode(PUSHER_MOTOR_PIN, OUTPUT);
	pinMode(PUSHER_BRAKE_PIN, OUTPUT);
	pinMode(FLYWHEEL_MOTOR_PIN, OUTPUT);
}

void loop() {

}